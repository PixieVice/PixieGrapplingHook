###########################################
# PixieGrapplingHook by the PixieWorkshop #
###########################################
##
#-- Options --#
##
GrapplingHook:
    speed: 2.0 # the force of the grappling hook #
    cooldown: 3 # in seconds #
    name: '&aGrappling Hook'
    lore: # Please use the § symbol when using color codes to lore lines #
        - '§7Let out the grappling hook then'
        - '§7reel it in to be pulled towards it!'
##
# In the recipe use '#' when defining an empty slot (aka air) #
Recipe:
  use: true
  materials:
    a: FISHING_ROD
    b: STRING
    c: STICK
  recipe:
    0: '  b'
    1: ' ab'
    2: 'c  '
##
# Fall damage will be negated as long as the player is holding the grappling hook #
DisableFallDamage: true
##
# Do Not Touch -_- #
config-version: 3